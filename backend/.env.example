DEBUG=True
SECRET_KEY=django-insecure-momentum-cybersecurity-mission-control-key-2026
ALLOWED_HOSTS=localhost,127.0.0.1

# Database Configuration (PostgreSQL by default, falls back to SQLite)
DB_ENGINE=django.db.backends.postgresql
DB_NAME=momentum_db
DB_USER=momentum_user
DB_PASSWORD=momentum_password
DB_HOST=localhost
DB_PORT=5432

# CORS Configuration
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000
